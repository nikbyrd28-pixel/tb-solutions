.DS_Store
.env
*.log

# Headless test suites live in the scratchpad but run with the repo root as cwd, so any
# artefact written to a relative path lands here. All suites now write absolute paths
# (fixed 14:16Z Wed); this is the backstop if a new one forgets.
/*-sample*.png
/*-preview*.png
/ad-*.png
/aaa-*.png
/fly*.png
/hopper.png
/game.png
/hockey.png
/ig-post.png
/ig-story.png
/logo-preview.png
/p-*.png
/signup.png
/skins-*.png
/spaz*.png

# Higgsfield CLI companion skills (installed with `npx skills add higgsfield-ai/skills`).
# Regenerable local tooling, not product code.
/.agents/
/.claude/skills/
/skills-lock.json
