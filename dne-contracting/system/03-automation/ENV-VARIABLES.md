# Environment variables (n8n → Settings → Variables)
| Name | Value |
|---|---|
| SITE_URL | https://your-vercel-domain |
| FROM_EMAIL | hello@dnecontracting.com (verified in Resend) |
| BIZ_PHONE | (484) 939-8535 |
| OWNER_NAME | mom's first name |
| MOM_WHATSAPP | whatsapp:+1484XXXXXXX |
| TWILIO_FROM | +1XXXXXXXXXX (bought number) |

Credentials to create in n8n: Supabase (Postgres, use the connection string from Project Settings → Database), Twilio (SID + token), Resend SMTP (smtp.resend.com, user `resend`, pass = API key).
