# D N E Contracting — website + business system

Live site source is this folder (`index.html` + `img/`). Vercel project `dne-contracting` deploys from it (root directory `dne-contracting`). It is also served by GitHub Pages at tbsol.net/dne-contracting/.

- `SETUP.md` — config block, Supabase portal wiring, n8n webhook, photo swap
- `system/` — full operating system: database schema, n8n workflows, marketing copy, GBP + listings kits, funnels, diagrams, gameplan. Start at `system/README.md`.

Before public launch: real PA HIC number in the config, mom's real photos (`img/`, same filenames), real reviews, `sampleContent:false`.
